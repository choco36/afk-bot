const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],tokenKey='afk.token';function showTab(n){$$('#tab-login,#tab-new,#tab-sessions,#tab-chat').forEach(el=>el.classList.add('hidden'));$('#tab-'+n).classList.remove('hidden');localStorage.setItem('afk.tab',n)}$$('.tab').forEach(b=>b.onclick=()=>showTab(b.dataset.tab));showTab(localStorage.getItem('afk.tab')||'login');function setLockUI(l){$('#lockDot').className='w-3 h-3 rounded-full inline-block '+(l?'bg-red-500':'bg-green-500');$('#lockText').textContent=l?'מצב: נעול – צריך להזין טוקן':'מצב: פתוח – הטוקן נשמר'}function authHeaders(){const t=localStorage.getItem(tokenKey);return t?{'Authorization':'Bearer '+t}:{}}async function checkLock(){try{const r=await fetch('/api/me');const {tokenRequired}=await r.json();setLockUI(!!tokenRequired&&!localStorage.getItem(tokenKey))}catch{}}$('#saveToken').onclick=()=>{localStorage.setItem(tokenKey,$('#token').value.trim());alert('נשמר');checkLock()};$('#stopAll').onclick=async()=>{if(!confirm('לעצור את כל הסשנים?'))return;try{const r=await fetch('/api/sessions/stop-all',{method:'POST',headers:authHeaders()});alert(r.ok?'הכול נעצר':'נכשל לעצור הכל');await refreshSessions()}catch(e){alert('נכשל לעצור הכל: '+(e?.message||e))}};checkLock();const api={async list(){const r=await fetch('/api/sessions',{headers:authHeaders()});if(!r.ok)throw new Error((await r.json()).error||r.statusText);return r.json()},async create(c){const r=await fetch('/api/sessions',{method:'POST',headers:{'Content-Type':'application/json',...authHeaders()},body:JSON.stringify(c)});if(!r.ok)throw new Error((await r.json()).error||r.statusText);return r.json()},async stop(id){const r=await fetch('/api/sessions/'+id+'/stop',{method:'POST',headers:authHeaders()});if(!r.ok)throw new Error((await r.json()).error||r.statusText);return r.json()},async logs(id){const r=await fetch('/api/sessions/'+id+'/logs',{headers:authHeaders()});if(!r.ok)throw new Error((await r.json()).error||r.statusText);return r.json()},async chat(id,text){const r=await fetch('/api/sessions/'+id+'/chat',{method:'POST',headers:{'Content-Type':'application/json',...authHeaders()},body:JSON.stringify({text})});if(!r.ok)throw new Error((await r.json()).error||r.statusText);return r.json()},};$('#l_auth').onchange=()=>$('#l_username').classList.toggle('hidden',$('#l_auth').value!=='offline');const loginConsole=$('#loginConsole');$('#loginForm').onsubmit=async e=>{e.preventDefault();const cfg={host:$('#l_host').value.trim(),port:Number($('#l_port').value||25565),auth:$('#l_auth').value,username:$('#l_auth').value==='offline'?$('#l_username').value.trim():undefined,loginOnly:true,autoReconnect:false};if(!cfg.host)return alert('צריך למלא כתובת שרת');if(cfg.auth==='offline'&&!cfg.username)return alert('Offline דורש שם משתמש');loginConsole.textContent+='\n[LOGIN] מתחבר…';try{const {id}=await api.create(cfg);loginConsole.textContent+='\n[LOGIN] מזהה: '+id;const poll=async()=>{try{const rows=await api.logs(id);const text=rows.map(r=>`[${new Date(r.ts).toLocaleTimeString()}] ${r.level.toUpperCase()} | ${r.msg}`).join('\n');if(text){loginConsole.textContent=text;loginConsole.scrollTop=loginConsole.scrollHeight}}catch{}};for(let i=0;i<30;i++){await new Promise(r=>setTimeout(r,1000));await poll()}loginConsole.textContent+='\n[LOGIN] אם דרוש אישור Microsoft — אשר בקישור שברשימה.'}catch(e2){loginConsole.textContent+='\n[LOGIN] נכשל: '+(e2?.message||e2)}};const sessionsBox=$('#sessions');function renderSessions(list){sessionsBox.innerHTML='';if(!list.length){sessionsBox.innerHTML='<div class="text-sm text-neutral-500">אין סשנים פעילים</div>';refreshChatSessions([]);return}list.forEach(s=>{const el=document.createElement('div');el.className='p-3 rounded-xl border bg-neutral-50 dark:bg-neutral-950 flex items-center justify-between gap-2';el.innerHTML=`<div class="min-w-0"><div class="font-medium truncate">${s.username||'(auth…)'} @ ${s.host}:${s.port}</div><div class="text-xs text-neutral-500">מצב: ${s.status} • ${s.auth} • AFK: ${s.afkMode}</div></div><div class="flex items-center gap-2"><button data-id="${s.id}" class="btn-stop px-3 py-2 rounded-lg bg-red-600 text-white">עצור</button><button class="copy-id px-3 py-2 rounded-lg border">העתק ID</button></div>`;sessionsBox.appendChild(el);el.querySelector('.btn-stop').onclick=async()=>{if(!confirm('לעצור סשן זה?'))return;try{await api.stop(s.id);await refreshSessions()}catch(e){alert('נכשל: '+(e?.message||e))}};el.querySelector('.copy-id').onclick=async()=>{await navigator.clipboard.writeText(s.id);alert('הועתק')}});refreshChatSessions(list)}async function refreshSessions(){try{renderSessions(await api.list())}catch(e){sessionsBox.innerHTML='<div class="text-sm text-red-600">שגיאת הרשאה (בדוק טוקן)</div>'}}$('#refreshSessions').onclick=refreshSessions;$('#auth').onchange=()=>$('#username').classList.toggle('hidden',$('#auth').value!=='offline');$('#sessionForm').onsubmit=async e=>{e.preventDefault();const cfg={host:$('#host').value.trim(),port:Number($('#port').value||25565),auth:$('#auth').value,username:$('#auth').value==='offline'?$('#username').value.trim():undefined,joinCmd:$('#joinCmd').value.trim(),keepAliveCmd:$('#keepAliveCmd').value.trim(),afkMode:$('#afkMode').value,afkIntervalMs:Number($('#afkIntervalMs').value||60000),autoReconnect:true,reconnectDelayMs:Number($('#reconnectDelayMs').value||10000)};if(!cfg.host)return alert('צריך למלא כתובת שרת');if(cfg.auth==='offline'&&!cfg.username)return alert('Offline דורש שם משתמש');try{const {id}=await api.create(cfg);alert('סשן התחיל: '+id);await refreshSessions()}catch(e2){alert('נכשל להתחיל: '+(e2?.message||e2))}};const chatBox=$('#chatBox'),chatSel=$('#chatSession');function refreshChatSessions(list){const old=chatSel.value;chatSel.innerHTML='<option value="">בחר סשן…</option>'+list.map(s=>`<option value="${s.id}">${s.username||'(auth…)'} @ ${s.host}</option>`).join('');if(list.some(s=>s.id===old))chatSel.value=old}$('#chatSend').onclick=async()=>{const id=chatSel.value,text=$('#chatInput').value.trim();if(!id)return alert('בחר סשן');if(!text)return;try{await api.chat(id,text);$('#chatInput').value=''}catch(e){alert('נכשל לשלוח: '+(e?.message||e))}};try{const proto=location.protocol==='https:'?'wss://':'ws://',tok=localStorage.getItem(tokenKey)||'',ws=new WebSocket(proto+location.host+'/ws'+(tok?('?token='+encodeURIComponent(tok)):''));ws.onmessage=ev=>{try{const {type,payload}=JSON.parse(ev.data);if(type==='sessions')renderSessions(payload);else if(type==='log'){const line=`[${new Date(payload.entry.ts).toLocaleTimeString()}] ${payload.entry.level.toUpperCase()} | ${payload.entry.msg}`;if(payload.entry.level==='chat'||payload.entry.level==='you'){chatBox.textContent=(chatBox.textContent?chatBox.textContent+'\n':'')+line;chatBox.scrollTop=chatBox.scrollHeight}const lc=$('#loginConsole');if(lc){lc.textContent=(lc.textContent?lc.textContent+'\n':'')+line;lc.scrollTop=lc.scrollHeight}}}catch{}}}catch{};refreshSessions();